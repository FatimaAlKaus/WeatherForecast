<Window x:Class="WeatherForecast.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        DataContext="{Binding Source={StaticResource VML},Path=MainWindowVM}"
        >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Button Grid.Row="1" Command="{Binding SendRequest }" HorizontalAlignment="Center" VerticalAlignment="Center">click</Button>
        <StackPanel>
            <Slider Value="{Binding Lon}" x:Name="lon" Minimum="-180" Maximum="180"></Slider>
            <Slider Value="{Binding Lat}" x:Name="lat" Minimum="-90" Maximum="90"></Slider>
            <TextBlock Text="{Binding Lon}"/>
            <TextBlock Text="{Binding Lat}"/>
        </StackPanel>
        <TextBox Grid.RowSpan="2" 
            Grid.Column="1" Text="{Binding Response}"></TextBox>
    </Grid>
</Window>
